#!/usr/bin/env node
/**
 * Theme exampleSite bootstrap (themes/airspace-hugo/scripts/projectSetup.js) is not used
 * for this project layout. Netlify or local scripts may still invoke `yarn project-setup`;
 * exit successfully so `yarn project-setup && yarn build` continues to Hugo.
 */
process.exit(0);
